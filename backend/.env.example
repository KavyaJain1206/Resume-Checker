# Leave MONGO_URL blank to use the built-in JSON file store (zero-config).
MONGO_URL=
DB_NAME=resume_arsenal
# Placement-admin credentials (Student Arsenal login)
ADMIN_EMAIL=admin@placement.team
ADMIN_PASSWORD=playbook2026
SECRET_KEY=change-me-in-prod
CORS_ORIGINS=http://localhost:5173
PORT=8000
